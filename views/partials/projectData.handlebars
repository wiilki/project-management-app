<div>

    <div>
        <h2> {{title}}</h2>
        <h3>Current progress: {{progress}} </h3>
    </div>

    <div>
        <p> {{description}}</p>
    </div>

    <div>
        <p>Posted by {{user.name}}</p>
        <p> Due by: {{format_date deadline}}</p>
    </div>

</div>