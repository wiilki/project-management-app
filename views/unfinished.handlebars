<div class="card">

    <div class="card-header">
        <h2> {{title}}</h2>
        <p>Posted by {{user.name}}. Due: {{format_date deadline}}</p>
    </div>

    <div class="card-body">
        <p> {{description}}</p>
    </div>

</div>